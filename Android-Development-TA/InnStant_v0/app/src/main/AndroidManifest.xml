<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.innstant">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Dashboard.ui.NotificationActivity"></activity>
        <activity android:name=".ui.RoomListed.EditRoomActivity" />
        <activity
            android:name=".ui.Auth.LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.Auth.SignUpActivity"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.Dashboard.DashboardMessageActivity"
            android:label="@string/title_activity_dashboard_message"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.Dashboard.DashboardNotificationActivity"
            android:label="@string/title_activity_dashboard_message"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.Rent.ApprovalActivity" />
        <activity android:name=".ui.Rent.SelectDateActivity" />
        <activity android:name=".ui.Rent.RoomDetailActivity" />
        <activity android:name=".ui.Rent.SetFIlterActivity" />
        <activity android:name=".ui.Rent.SearchDetailActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".ui.Rent.FindingNearbyRoomsActivity"
            android:label="@string/title_activity_finding_nearby_rooms" />
        <activity
            android:name=".ui.Rent.RentRoomActivity"
            android:label="@string/title_activity_rent_room"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.HostRoom.BookingApprovalActivity" />
        <activity android:name=".ui.HostRoom.AddPictureActivity" />
        <activity android:name=".ui.HostRoom.SetRoomPricingActivity" />
        <activity android:name=".ui.RentStatus.ReservedActivity" />
        <activity android:name=".ui.RentStatus.idleActivity" />
        <activity android:name=".ui.RoomListed.RoomDetailActivity" />
        <activity android:name=".ui.RoomListed.ListedRoomActivity" />
        <activity android:name=".ui.HostRoom.SetAvaliabilityActivity" />
        <activity android:name=".ui.HostRoom.GeneralDescriptionActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <!-- <meta-data -->
        <!-- android:name="OnlineMaps.Key" -->
        <!-- android:value="@string/online_maps_key" /> -->
        <!-- <meta-data -->
        <!-- android:name="OnlineSearch.Key" -->
        <!-- android:value="@string/online_search_key" /> -->
        <activity
            android:name=".ui.SetLocationActivity"
            android:label="@string/title_activity_set_location" />
        <activity
            android:name=".ui.RoomHostingActivity"
            android:label="@string/title_activity_room_hosting"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.DashboardActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>